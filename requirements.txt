# Streamlit Community Cloud installed 1.61.0 from the previous range, whose
# Starlette GZip middleware failed its health check. 1.58.0 is the version
# exercised by this project's local app and AppTest suite.
streamlit==1.58.0
# Cloud otherwise resolves Starlette 1.4.0, whose GZipResponder constructor is
# incompatible with Streamlit 1.58.0. This is the locally verified pairing.
starlette==1.3.1
pandas>=2.0
numpy>=1.24
plotly>=6.0,<7
# App (deploy) dependencies only. Build and reproduction tools live in
# requirements-dev.txt. Install both files for a clean-machine reproduction; deploy
# only this file so the app loads precomputed results with a fast cold start.
